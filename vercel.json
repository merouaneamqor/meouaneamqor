{
    "buildCommand": "pnpm run build",
    "outputDirectory": ".output",
    "framework": "nuxt",
    "builds": [
      {
        "src": "nuxt.config.ts",
        "use": "@nuxtjs/vercel-builder@1.0.2"
      }
    ],
    "routes": [
      {
        "src": "/.*",
        "dest": ".output/server/index.mjs"
      }
    ],
    "env": {
      "NITRO_PRESET": "vercel"
    }
  }